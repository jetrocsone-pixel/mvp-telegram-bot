import json
import logging

from openai import OpenAI

from app.config import OPENAI_API_KEY

logger = logging.getLogger(__name__)
MODEL_NAME = "gpt-4o-mini"
client = OpenAI(api_key=OPENAI_API_KEY) if OPENAI_API_KEY else None


def _get_client():
    if client is None:
        raise RuntimeError("Не задан OPENAI_API_KEY.")

    return client


def _build_image_content(prompt, image_urls):
    content = [{"type": "input_text", "text": prompt}]

    for image_url in image_urls:
        content.append({"type": "input_image", "image_url": image_url})

    return content


def _create_response(content):
    return _get_client().responses.create(
        model=MODEL_NAME,
        input=[
            {
                "role": "user",
                "content": content
            }
        ]
    )


def _parse_questions_response(raw_text):
    try:
        parsed = json.loads(raw_text)
    except json.JSONDecodeError as exc:
        logger.exception("OpenAI returned invalid JSON for TZ Pro questions")
        raise ValueError("OpenAI вернул некорректный JSON для вопросов.") from exc

    questions = parsed.get("questions")
    if not isinstance(questions, list):
        raise ValueError("OpenAI вернул ответ без списка questions.")

    return parsed


def generate_tz_lite(image_url):
    prompt = """
Ты — эксперт по созданию продающих фотоворонок для маркетплейсов (Wildberries, Ozon).

Задача: по ОДНОЙ фотографии товара создать ТЗ для дизайнера.

---

1) ОПРЕДЕЛИ (внутри, не выводить):

- что это за товар
- его назначение
- целевую аудиторию (только очевидные выводы)

---

2) ОПРЕДЕЛИ ПОВЕДЕНЧЕСКИЙ ТИП ТОВАРА (НЕ ВЫВОДИТЬ):

- одежда
- визуал / эмоция
- рационал / техника
- утилитарка / решение проблемы
- дом / мебель
- товарка / подарок

И строй воронку, исходя из логики этого типа:

- одежда → посадка / образ / детали / размер
- техника → характеристики / применение / совместимость / безопасность
- утилитарка → проблема / решение / процесс / результат
- мебель → визуал / функция / габариты / доверие
- товарка → эстетика / атмосфера / упаковка
- визуал → “хочу” через картинку

---

3) ОПРЕДЕЛИ 3 КЛЮЧЕВЫХ СМЫСЛА

(главное, что продаёт товар)

Именно они должны лечь в основу первых слайдов.

---

4) СФОРМИРУЙ ВОРОНКУ ЧЕРЕЗ УГЛЫ ПОДАЧИ

Используй 4–6 разных углов:

- результат / эффект
- проблема → решение
- сценарий использования
- демонстрация процесса
- сравнение
- конструкция / внутрянка
- параметры / габариты
- универсальность
- премиальность / стиль
- минимализм (чистый визуал)
- детали / качество
- безопасность / надежность
- упаковка / доставка
- гарантия / доверие

ВАЖНО:
- первые 5 слайдов = 5 разных углов
- не повторять одинаковые смыслы
- не использовать шаблонную структуру

---

5) ОПРЕДЕЛИ КОЛИЧЕСТВО СЛАЙДОВ:

- простой товар → 5–6
- средний → 6–8
- сложный / техника / одежда → 8–10

---

6) СФОРМИРУЙ ТЗ

🔹 СЛАЙДЫ 1–5 (ПОДРОБНО)

Слайд 1 — ...
Слайд 2 — ...
Слайд 3 — ...
Слайд 4 — ...
Слайд 5 — ...

Для каждого:

- Что изображено:
- Текст на изображении:
- Цель слайда:

ТРЕБОВАНИЯ К ТЕКСТУ:
- 1–2 короткие строки
- конкретика (не “высокое качество”)
- читается за 1–2 секунды
- без клише

ЕСЛИ ВИЗУАЛ СИЛЬНЫЙ:
- можно без текста

---

🔹 СЛАЙДЫ 6+ (КРАТКО)

Формат строго:

Слайд 6 — ...
Слайд 7 — ...
Слайд 8 — ...

(1 строка = 1 смысл, без пояснений)

Эти слайды должны:
- закрывать возражения
- давать конкретику
- усиливать доверие

---

7) ОБЯЗАТЕЛЬНЫЕ БЛОКИ (ЕСЛИ УМЕСТНЫ)

НЕ писать точные цифры, но указывать:

- размеры / габариты
- характеристики / мощность / объем
- комплектация
- упаковка
- гарантия / возврат
- инструкция / применение

---

8) ПРАВИЛА (ЖЁСТКО)

ЗАПРЕЩЕНО:
- выдумывать характеристики
- использовать один шаблон
- повторять структуру
- делать одинаковые по смыслу слайды подряд

НЕ ДЕЛАТЬ:
- “обложка → преимущества → размеры” без анализа

КАЖДЫЙ РАЗ:
- новая структура
- новая комбинация углов
- новая логика

---

9) ПРОВЕРКА

Перед ответом:
- убери выдуманные факты
- убери слабые формулировки
- проверь, что слайды разные по смыслу

---

10) ДОПОЛНИТЕЛЬНО

Альтернативная концепция (кратко):

Слайд 1 — ...
Слайд 2 — ...
Слайд 3 — ...
Слайд 4 — ...

(другой угол подачи, без повторения основной логики)

---

Ответ:
четкий, структурный, без лишнего текста.
"""

    response = _create_response(_build_image_content(prompt, [image_url]))

    return response.output_text


def generate_tz_pro_questions(image_urls):
    prompt = """
Ты анализируешь товар по изображениям.

Твоя задача — сформировать 6 вопросов пользователю, чтобы уточнить данные для создания фотоворонки.

---

СТРУКТУРА:

Нужно вернуть:

- 4 фиксированных вопроса (всегда одинаковые)
- 2 умных вопроса (генерируются под товар)

---

ФИКСИРОВАННЫЕ ВОПРОСЫ:

Вопрос 1 — Что главное нужно подчеркнуть?

A. Внешний вид / дизайн
B. Функцию / пользу
C. Удобство / комфорт
D. Результат / эффект

Вопрос 2 — Как лучше показать товар?

A. В использовании
B. В окружении
C. Через детали
D. Смешанно

Вопрос 3 — Какой формат подачи нужен?

A. Минимализм
B. Инфографика
C. Эмоция / атмосфера
D. Смешанный

Вопрос 4 — Какой блок обязательно нужен?

A. Размеры / габариты
B. Характеристики / состав
C. Использование / сценарии
D. Комплектация

---

УМНЫЕ ВОПРОСЫ (5 и 6):

Требования:

- должны закрывать пробелы, которые нельзя понять по фото
- должны влиять на ТЗ
- должны быть конкретными
- каждый вопрос должен иметь 4 варианта ответа A/B/C/D
- не должны дублировать фиксированные вопросы
- не должны быть абстрактными
- не должны быть формальными

---

ЗАПРЕЩЕНО:

- задавать абстрактные вопросы
- задавать вопросы без влияния на визуал
- дублировать фиксированные вопросы

---

ФОРМАТ ОТВЕТА:

Верни строго JSON, без пояснений, без markdown, без текста до или после JSON.

Структура должна быть ровно такой:

{
  "questions": [
    {
      "question": "Что главное нужно подчеркнуть?",
      "options": {
        "A": "Внешний вид / дизайн",
        "B": "Функцию / пользу",
        "C": "Удобство / комфорт",
        "D": "Результат / эффект"
      }
    },
    {
      "question": "Как лучше показать товар?",
      "options": {
        "A": "В использовании",
        "B": "В окружении",
        "C": "Через детали",
        "D": "Смешанно"
      }
    },
    {
      "question": "Какой формат подачи нужен?",
      "options": {
        "A": "Минимализм",
        "B": "Инфографика",
        "C": "Эмоция / атмосфера",
        "D": "Смешанный"
      }
    },
    {
      "question": "Какой блок обязательно нужен?",
      "options": {
        "A": "Размеры / габариты",
        "B": "Характеристики / состав",
        "C": "Использование / сценарии",
        "D": "Комплектация"
      }
    },
    {
      "question": "...",
      "options": {
        "A": "...",
        "B": "...",
        "C": "...",
        "D": "..."
      }
    },
    {
      "question": "...",
      "options": {
        "A": "...",
        "B": "...",
        "C": "...",
        "D": "..."
      }
    }
  ]
}
"""

    response = _create_response(_build_image_content(prompt, image_urls))

    raw_text = response.output_text.strip()
    return _parse_questions_response(raw_text)

def generate_tz_pro_result(image_urls, answers, mode, base_prompt, mode_prompt):
    answers_text = "\n".join(
        [
            f"{index + 1}. Вопрос: {item['question']}\n"
            f"Ответ: {item['selected_option']} — {item['selected_text']}"
            for index, item in enumerate(answers)
        ]
    )

    content = [
        {
            "type": "input_text",
            "text": (
                f"{base_prompt}\n\n"
                f"{mode_prompt}\n\n"
                f"ОТВЕТЫ ПОЛЬЗОВАТЕЛЯ:\n{answers_text}\n\n"
                f"Сформируй готовое ТЗ."
            )
        }
    ]

    for image_url in image_urls:
        content.append({"type": "input_image", "image_url": image_url})

    response = _create_response(content)

    return response.output_text
